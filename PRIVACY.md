# Privacy

File Color Rules does not collect, store, or transmit personal data, usage statistics, analytics, or telemetry.

The plugin performs no runtime network requests. It reads and writes only `.jetbrains-file-colors.yaml` in projects where it is used and reads lightweight Virtual File metadata needed to evaluate structural rules.
